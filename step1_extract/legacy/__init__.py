"""
Legacy Parsers Module

Contains old vendor-specific parsers (costco_parser.py, rd_parser.py) that
are kept for backward compatibility but should not be used in new code.

New code should use receipt_line_engine.py which applies YAML patterns.
"""

