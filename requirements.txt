# Receipt Importer Requirements
# Rule-driven receipt processing system

# ============================================================================
# CORE DEPENDENCIES (Required)
# ============================================================================

# YAML parsing for rule files (Step 1 configuration)
PyYAML>=6.0.1

# Excel file processing (Costco, RD, Jewel-Osco, Aldi, BBI, etc.)
pandas>=2.0.0
openpyxl>=3.1.0

# PDF Processing (multiple libraries for fallback)
PyPDF2>=3.0.1
PyMuPDF>=1.23.0
pdfplumber>=0.10.0
pdfminer.six>=20221105

# Web scraping for vendor knowledge base lookup
requests>=2.31.0
beautifulsoup4>=4.12.0

# ============================================================================
# DATABASE (Step 2 & 3)
# ============================================================================

# PostgreSQL adapter for Odoo database operations
psycopg2-binary>=2.9.9

# ============================================================================
# OPTIONAL DEPENDENCIES
# ============================================================================

# PDF Generation (auto-convert HTML reports to PDF)
playwright>=1.40.0
# After install, run: playwright install chromium

# Image processing (for OCR)
Pillow>=10.2.0

# OpenCV for advanced image preprocessing (binarization, deskewing, scaling)
opencv-python>=4.5.0

# OCR for image-based PDFs (optional, requires tesseract system dependency)
pytesseract>=0.3.10
# After install, install tesseract:
# macOS: brew install tesseract
# Ubuntu/Debian: sudo apt-get install tesseract-ocr
# Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki

# ============================================================================
# PYTHON VERSION
# ============================================================================

# Requires: Python 3.8+
# Tested on: Python 3.8, 3.9, 3.10, 3.11

# ============================================================================
# INSTALLATION
# ============================================================================

# Basic install:
#   pip install -r requirements.txt

# With PDF generation support:
#   pip install -r requirements.txt
#   playwright install chromium

# ============================================================================
# NOTES
# ============================================================================

# - All rule logic is in YAML files (step1_rules/)
# - No AI/LLM dependencies (100% rule-based)
# - PDF generation is optional (HTML reports always generated)
# - macOS users: If psycopg2-binary 2.9.10+ fails, use 2.9.9

